### Keybindings
$mainMod = SUPER

# Trigger when the switch is turning on (lid closes)
bindl = , switch:on:Lid Switch, exec, ~/.config/hypr/lid-close.sh
# Trigger when the switch is turning off (lid opens)
bindl = , switch:off:Lid Switch, exec, ~/.config/hypr/lid-open.sh

# Launch terminal
bind = $mainMod, Return, exec, alacritty

# Show keybindings
bind = $mainMod, K, exec, ~/.config/hypr/show-bindings.sh

# Launch application launcher
bind = $mainMod, Space, exec, rofi -show drun

# Launch file manager
bind = $mainMod, E, exec, alacritty --class yazi -e yazi

# Close window
bind = $mainMod, Q, killactive,

# Toggle floating mode
bind = $mainMod, F, togglefloating,

# Copy/Paste (universal clipboard shortcuts that work everywhere)
# Uses Ctrl+Insert and Shift+Insert which work in terminals AND GUI apps
# Note: Trailing comma targets active window
bind = $mainMod, C, sendshortcut, CTRL, c,
bind = $mainMod, V, sendshortcut, CTRL, v,

# Reload config (smart reload preserves monitor setup)
bind = $mainMod SHIFT, R, exec, ~/.config/hypr/smart-reload.sh

# Exit Hyprland (closes all windows first)
bind = $mainMod SHIFT, Q, exec, ~/.config/hypr/exit-hyprland.sh

# Lock and suspend
bind = $mainMod SHIFT, L, exec, swaylock -f

# Toggle hypridle (gaming mode)
bind = $mainMod, G, exec, ~/.config/hypr/toggle-hypridle.sh

# Screenshots
bind = $mainMod SHIFT, S, exec, grim -g "$(slurp -w 0)" - | wl-copy && notify-send "Screenshot copied" "Copied to clipboard"
bind = , Print, exec, grim ~/Pictures/screenshot-$(date +%Y%m%d-%H%M%S).png && notify-send "Screenshot saved" "Saved to ~/Pictures/"
bind = $mainMod SHIFT, E, exec, grim -g "$(slurp -w 0)" - | swappy -f -

# Screen recording (10 seconds of selected area)
bind = $mainMod SHIFT, V, exec, ~/.config/hypr/screenrecord.sh

### Workspace Switching
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9

# Move active window to workspace
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9

### Focus & Movement
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

bind = $mainMod SHIFT, left, movewindow, l
bind = $mainMod SHIFT, right, movewindow, r
bind = $mainMod SHIFT, up, movewindow, u
bind = $mainMod SHIFT, down, movewindow, d

### Volume Controls
bind = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%- && pw-play /run/current-system/sw/share/sounds/freedesktop/stereo/message.oga
bind = , XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+ && pw-play /run/current-system/sw/share/sounds/freedesktop/stereo/message.oga
bind = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle

# Audio device switcher
bind = $mainMod, A, exec, ~/.config/hypr/audio-switcher.sh

### Media Controls
bind = , XF86AudioPlay, exec, playerctl play-pause
bind = , XF86AudioPause, exec, playerctl play-pause
bind = , XF86AudioNext, exec, playerctl next
bind = , XF86AudioPrev, exec, playerctl previous

### Mouse Bindings
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
